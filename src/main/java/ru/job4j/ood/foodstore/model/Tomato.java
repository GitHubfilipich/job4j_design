package ru.job4j.ood.foodstore.model;

import java.util.Calendar;

public class Tomato extends Food {
    public Tomato(String name, Calendar createDate, Calendar expiryDate) {
        super(name, createDate, expiryDate);
    }
}
